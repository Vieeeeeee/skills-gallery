# Skills Gallery Environment Configuration
# Copy this file to .env or .env.local to customize your settings

# 编辑模式口令（不设则用 wibi888）
# 注意：VITE_ 前缀的变量会被打进公开的前端 bundle，任何人都能从 JS 里读到。
# 它只是"防手滑"，不是安全边界——所有编辑都只写当前浏览器的 localStorage，
# 不会影响其他访客看到的内容。真要做权限控制，必须放到服务端。
VITE_ADMIN_PASSCODE=wibi888
